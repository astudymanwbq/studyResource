<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wbq.spring.dao.RedPacketDao">
    <select id="getRedPacket" parameterType="long" resultType="com.wbq.spring.RedPacket">
        select id,
        user_id userId,
        amount,
        send_date sendDate,
        total,
        unit_amount unitAmount,
        stock,
        version,
        note
        from t_red_packet
        where id=#{id} /*for update  悲观锁*/
    </select>

    <update id="decreaseRedPacket">
        update t_red_packet set stock=stock-1 where id=#{id}
    </update>
    <update id="decreaseRedPacket_1">
        update t_red_packet set stock=stock-1,version=version+1 where id=#{id}
    </update>
</mapper>